/**
 * Student-side THPT exam types (re-export base types + UI helpers)
 */
export type {
  ThptConfig,
  ThptSection,
  SectionType,
  ThptAnswers,
  ThptResult,
  ThptResultSectionStat,
  ThptOption,
  ThptTfStatement,
} from '../../../../../types/thpt';

export type ViewMode = 'taking' | 'review';
